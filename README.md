# powertop-autotune.service

A simple arch package that adds the systemd service to auto-enable all tunables at boot time.

## How to use
    systemctl start powertop.service
    systemctl enable powertop.service
